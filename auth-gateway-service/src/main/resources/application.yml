server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres-db:5432/techvalley_db
    username: techvalley_admin
    password: password123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect