version: '3.8'

services:
  # 1. Database Postgres
  postgres-db:
    image: postgres:15-alpine
    container_name: techvalley-postgres
    environment:
      POSTGRES_USER: techvalley_admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: techvalley_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - techvalley-net
  
  pgadmin:
    image: dpage/pgadmin4
    container_name: techvalley-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@techvalley.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      - postgres-db
    networks:
      - techvalley-net

  # 2. Auth & Gateway Service (Spring Boot)
  auth-gateway-service:
    build:
      context: .
      dockerfile: auth-gateway-service/Dockerfile
    container_name: auth-gateway-service
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres-db:5432/techvalley_db
      - SPRING_DATASOURCE_USERNAME=techvalley_admin
      - SPRING_DATASOURCE_PASSWORD=password123
    depends_on:
      - postgres-db
    networks:
      - techvalley-net

  # 3. Core Cloud Monitor Service (Spring Boot)
  cloud-monitor-service:
    build:
      context: .
      dockerfile: cloud-monitor-service/Dockerfile
    container_name: cloud-monitor-service
    ports:
      - "8081:8081"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres-db:5432/techvalley_db
      - SPRING_DATASOURCE_USERNAME=techvalley_admin
      - SPRING_DATASOURCE_PASSWORD=password123
    depends_on:
      - postgres-db
    networks:
      - techvalley-net

  # 4. LLM Service (Spring Boot)
  llm-service:
    build:
      context: .
      dockerfile: llm-service/Dockerfile
    container_name: llm-service
    ports:
      - "8082:8082"
    networks:
      - techvalley-net

  # 5. Frontend Demo (Nginx Web Server)
  frontend-demo:
    build: ./frontend-demo
    container_name: frontend-demo
    ports:
      - "80:80"
    depends_on:
      - auth-gateway-service
    networks:
      - techvalley-net

networks:
  techvalley-net:
    driver: bridge

volumes:
  pgdata: